% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectories.R
\name{et_trajectories}
\alias{et_trajectories}
\title{Read archived trajectories back into R.}
\usage{
et_trajectories(fit, draws = NULL)
}
\arguments{
\item{fit}{An \code{\link[=et_fit]{et_fit()}} from \code{\link[=et_sample]{et_sample()}} run with \verb{save_x =}, or a path
template.}

\item{draws}{Which archived draws to read (indices into the archive, in sweep
order). Defaults to all of them.}
}
\value{
A list of integer matrices.
}
\description{
Reads the trajectories \code{\link[=et_sample]{et_sample()}} archived, as a list of
\verb{n_timepoints x n_individuals} integer matrices of 1-based state codes.
}
\details{
Selecting \code{draws} reads only the chunks that contain them, so inspecting a
handful out of thousands does not read the whole archive. Reading them all is
what the archive exists to avoid: \code{\link[=et_residuals]{et_residuals()}} scores them one chunk at
a time without ever materialising the run, so pass \code{draws} unless the
archive is small.
}
