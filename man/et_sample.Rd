% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{et_sample}
\alias{et_sample}
\title{Fit the model.}
\usage{
et_sample(
  model,
  blocks = list(),
  n_sweeps = 1000,
  n_burn = 0,
  n_adapts = 0,
  seed = 1,
  x_init = NULL,
  adtype = "forwarddiff",
  save_x = NULL,
  save_every = 100,
  convert_x = TRUE,
  keep_source = NULL,
  quiet = FALSE
)
}
\arguments{
\item{model}{An \code{\link[=et_model]{et_model()}}.}

\item{blocks}{List of sampler blocks (\code{\link[=et_nuts]{et_nuts()}}, \code{\link[=et_hmc]{et_hmc()}},
\code{\link[=et_conjugate]{et_conjugate()}} and friends). Any parameter not named in a block is
collected into one NUTS block, and the trajectory always gets an iFFBS
kernel -- so omitting \code{blocks} entirely still gives a valid sampler.}

\item{n_sweeps}{Kept Gibbs sweeps.}

\item{n_burn}{Sweeps discarded before collection.}

\item{n_adapts}{Adaptation steps passed to every HMC/NUTS block.}

\item{seed}{RNG seed.}

\item{x_init}{Optional starting trajectory, an \verb{n_timepoints x n_individuals} integer matrix of 1-based state codes. Defaults to
all-first-state.}

\item{adtype}{The automatic-differentiation backend: a backend name or an
\code{\link[=et_adtype]{et_adtype()}}. Forward mode (the default) costs roughly one pass per CHUNK
of parameters, so it rewards small blocks; reverse mode costs one pass
whatever the block size. It changes only the time taken, never the answer.}

\item{save_x}{Optional path (e.g. \code{"X.rds"}) to stream the latent trajectory
to during sampling. Required for \code{\link[=et_residuals]{et_residuals()}}. The trajectory is always
kept live for conditioning; this only decides where the per-sweep copy goes,
and by default it is dropped rather than carried in the chain -- it is
\verb{n_timepoints x n_individuals} integers EVERY sweep.

The archive is written as \code{.rds}, which R reads natively. Sampling streams
through Julia's own format and \code{\link[=et_convert_trajectories]{et_convert_trajectories()}} rewrites it once
sampling finishes; pass a \code{.jld2} path to skip that and keep the Julia
files. Either way the archive is one file per flush, not one huge file.}

\item{save_every}{Flush the archive every this many sweeps.}

\item{convert_x}{Convert the archive to \code{.rds} after sampling. Only consulted
when \code{save_x} names a \code{.rds} path. Set \code{FALSE} to defer it -- the fit still
records where the archive is, so \code{\link[=et_convert_trajectories]{et_convert_trajectories()}} can be run
later.}

\item{keep_source}{Optional path to write the generated Julia to.}

\item{quiet}{Suppress progress messages.}
}
\value{
An object of class \code{et_fit}.
}
\description{
Fit the model.
}
