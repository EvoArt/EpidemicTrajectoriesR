% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfo.R
\name{et_capture_recapture}
\alias{et_capture_recapture}
\title{Score a capture-recapture window.}
\usage{
et_capture_recapture(
  caught,
  p,
  unobservable_states = "absorbing",
  test = NULL,
  infected_states = NULL,
  sensitivity = NULL,
  specificity = NULL
)
}
\arguments{
\item{caught}{Name of an \code{extras} entry holding an
\verb{n_timepoints x n_individuals} 0/1 capture matrix.}

\item{p}{Name of the model parameter giving the capture probability of a live
individual.}

\item{unobservable_states}{States in which an individual cannot be caught, so
that a capture contradicts the trajectory outright. Either integer state
codes, or \code{"absorbing"} (the default) to use whichever state the model's
declared transitions make absorbing: death, in the usual case. \code{NULL} for
a model where every state is observable, when the capture term alone is the
whole density.

\code{"absorbing"} is derived, not declared, so it cannot fall out of step with
the transitions. Note that the \emph{constraint} can only ever forbid the
absorbing state (see \code{constrain_survival} in \code{\link[=et_lfo_spec]{et_lfo_spec()}}), while this
argument may name any number of states: a trajectory can be contradicted
by a state the proposal has no way to avoid.}

\item{test}{Optional name of an \code{extras} entry holding the test result for a
caught individual (\code{1} positive, \code{0} negative, anything else "not tested").
Only read where \code{caught} is 1.}

\item{infected_states}{Integer codes of the states a test can detect.}

\item{sensitivity, specificity}{Names of the model parameters for the test's
sensitivity and specificity. \code{specificity} may be left \code{NULL} for a test
with no false positives.}
}
\value{
An object of class \code{et_capture_recapture}, accepted by
\code{\link[=et_lfo_spec]{et_lfo_spec()}} as \code{cell_logdensity}.
}
\description{
The observation model a mark-recapture study has: a dead individual is never
seen, a live one is caught with probability \code{p}, and if \code{test} is given,
a caught individual is also tested, so the observation carries information
about its infection state and not merely that it was alive.
}
\details{
\strong{Supply \code{test} whenever the comparison is about transmission.} Capture
alone separates alive from dead and nothing else, so every infection state
has the same observation weight; the held-out score then barely depends on
the transmission model, and an LFO comparison between transmission models
has almost nothing to work with.

Supplying this instead of a hand-written \code{\link[=et_julia]{et_julia()}} also fixes the two
things easiest to get wrong: the density is \code{-Inf} only where the data
actually contradict the trajectory, and the survival constraint is derived
from the same capture matrix, so the two can never disagree about who was
alive.
}
