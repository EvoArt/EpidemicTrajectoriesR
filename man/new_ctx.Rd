% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transpile.R
\name{new_ctx}
\alias{new_ctx}
\title{Create a transpiler context.}
\usage{
new_ctx(
  helpers = character(),
  vector_result = FALSE,
  state_syms = FALSE,
  states = character(),
  reads = NULL
)
}
\arguments{
\item{helpers}{Character vector of user helper names callable from a body.}

\item{vector_result}{\code{TRUE} inside a role that returns a per-state vector, in
which \code{rep(1, n)} / \code{numeric(n)} become \code{ones(ETR_T, n)} / \code{zeros(ETR_T, n)}
so the allocation follows the parameter scalar type (see DESIGN.md 3.3).}

\item{state_syms}{\code{TRUE} where a string naming a state should be resolved
(aggregate update bodies): against \code{state} it becomes a Julia Symbol,
against \code{X[...]} the state's integer code.}

\item{states}{The state space, in encoding order. Needed to resolve a name
compared against the trajectory to its code.}

\item{reads}{An environment accumulating the \code{model$x} names read.}
}
\description{
Create a transpiler context.
}
\keyword{internal}
