% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adtype.R
\name{et_adtype}
\alias{et_adtype}
\title{Choose the automatic-differentiation backend.}
\usage{
et_adtype(
  name = c("forwarddiff", "polyester", "reversediff", "mooncake", "enzyme"),
  chunksize = NULL,
  args = list()
)
}
\arguments{
\item{name}{One of \code{"forwarddiff"} (the default), \code{"polyester"},
\code{"reversediff"}, \code{"mooncake"}, \code{"enzyme"}.}

\item{chunksize}{Forward-mode chunk size. \code{NULL} lets the backend choose.
Cost is roughly linear in the number of CHUNKS, not in the parameter count,
so this interacts strongly with how you block the parameters.}

\item{args}{Extra named arguments spliced into the \code{ADTypes} constructor.}
}
\value{
An object of class \code{et_adtype}.
}
\description{
Choose the automatic-differentiation backend.
}
\details{
\strong{Forward mode} (\code{forwarddiff}, \code{polyester}) carries the derivative alongside
the value, so during a gradient every parameter is a \code{ForwardDiff.Dual}. Cost
scales with the number of chunk passes over the parameters, which makes it
strong for small blocks and weak for large ones. \code{polyester} is the threaded
variant and is what the badger model was tuned with.

\strong{Reverse mode} (\code{mooncake}, \code{enzyme}, \code{reversediff}) computes all partials
in one reverse pass, so it does not care how many parameters a block has --
the usual choice once a block is large. \code{mooncake} and \code{enzyme} leave the
primal as plain \code{Float64}; \code{reversediff} uses a tracked scalar type.

Backends other than the two forward ones are not installed by default; add
them with \code{et_setup(ad_backends = "mooncake")}, which resolves them into this
package's pinned Julia project.

No backend changes the answer -- only the time taken. If two backends disagree
on a log density or a gradient, that is a bug, not a tuning question.
}
