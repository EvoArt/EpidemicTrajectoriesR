% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{prior}
\alias{prior}
\alias{et_par}
\title{Declare a model parameter and its prior.}
\usage{
prior(dist = NULL, init, n = 1L, dim = NULL, kind = c("sampled", "latent"))

et_par(...)
}
\arguments{
\item{dist}{The prior distribution: \code{\link[=beta_dist]{beta_dist()}}, \code{\link[=gamma_dist]{gamma_dist()}} and
friends, or \code{\link[=custom_dist]{custom_dist()}} for anything else. Omit only for
\code{kind = "latent"}.}

\item{init}{Initial value. Length must match \code{n} (or \code{prod(dim)}).}

\item{n}{Length of a vector parameter; \code{1} for a scalar. A vector parameter is
emitted as \code{PracticalBayes.filldist(dist, n)}.}

\item{dim}{Dimensions of a matrix-valued parameter (\code{kind = "latent"} only).}

\item{kind}{\code{"sampled"} (the default) for a parameter with a prior, or
\code{"latent"} for one \strong{owned by a conjugate kernel}: emitted as a
placeholder distribution whose density is constant, exactly as the badger
model's \code{nu} is.}
}
\value{
An object of class \code{prior}.
}
\description{
Declare a model parameter and its prior.
}
\examples{
parameters <- list(
  alpha = prior(gamma_dist(2, 0.005), init = 0.005),
  p     = prior(beta_dist(2, 2),      init = 0.6))
}
