% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfo.R
\name{et_lfo_spec}
\alias{et_lfo_spec}
\title{A leave-future-out scoring specification.}
\usage{
et_lfo_spec(
  cell_logdensity,
  truncation,
  is_informative = NULL,
  constrain_survival = FALSE,
  known_present = NULL,
  n_sim = 1L,
  seed = 13L
)
}
\arguments{
\item{cell_logdensity}{Either an \code{\link[=et_capture_recapture]{et_capture_recapture()}} (the usual case)
which also derives \code{known_present} for you, or an \code{\link[=et_julia]{et_julia()}} wrapping a
Julia function \verb{(model, data, X, i, t) -> Float64}: individual \code{i}'s
observation log-density at \code{t} given the simulated states \code{X}. Return
\code{-Inf} for an inadmissible trajectory; it is charged to that individual's
own cell, never to the whole window.}

\item{truncation}{An \code{\link[=et_lfo_truncation]{et_lfo_truncation()}}.}

\item{is_informative}{Optional \code{\link[=et_julia]{et_julia()}} wrapping
\verb{(data, i, t) -> Bool}: whether that cell's density actually depends on the
latent state. Strongly recommended, see \code{n_informative()} in ET's
\code{lfo.jl}: on real data this can be a small fraction of the nominal cell
count, which is the difference between "no granularity discriminates much"
and a real comparison.}

\item{constrain_survival}{Simulate from the survival-constrained proposal
instead of the raw dynamics: never enter the absorbing state when the data
prove the individual was still present, and accumulate the importance
weight that converts back to the true kernel. \code{TRUE} corrects the wasted
draws; \code{FALSE} is the unconstrained estimator, kept so the two can be run
on identical draws and compared.

\strong{This addresses the absorbing state only, which is narrower than the
problem it belongs to.} A draw scores \code{-Inf} whenever the simulated
trajectory contradicts an observation, and an absorbing state is merely the
worst case: once entered it cannot be left, so one bad step condemns the
whole draw. A transient state that the observation also rules out — a
recovered individual that later tests positive, say — produces the same
\code{-Inf}, and this correction does nothing about it, because "do not enter
state k at time t" is only a well-defined restriction with a computable
weight when k is absorbing. Where transient states are the binding
constraint, reach for a finer granularity (so the loss is charged to one
cell rather than the window) or an exact forward recursion.

With an \code{\link[=et_capture_recapture]{et_capture_recapture()}} score this needs nothing further: who was
alive is read off the same capture matrix, looking ahead \strong{only to the end
of the scored window}. That bound matters. A capture after \code{t + M} is
outside the block being scored, and conditioning on it deletes a death
branch carrying real probability mass: a lost positive contribution that
no reweighting restores. Otherwise supply \code{known_present}, and bound it
yourself.}

\item{known_present}{Optional \code{\link[=et_julia]{et_julia()}} whose body is a function of \code{i}
and \code{t} only: individual \code{i} is known to be alive at \code{t}. \strong{\code{data} is not
in scope}: ET calls this as \code{known_present(i, t)}, so reach arrays through
the generated module's own \code{DATA}, e.g. \code{t <= DATA.last_seen[i]}. Ignored
unless \code{constrain_survival} is \code{TRUE}.}

\item{n_sim}{Forward trajectories per posterior draw per window.}

\item{seed}{Base RNG seed for the forward simulation.}
}
\value{
An object of class \code{et_lfo_spec}.
}
\description{
R mirror of ET's \code{LFOSpec}. \code{cell_logdensity} (and \code{constrain}, if given) are
written directly in Julia via \code{\link[=et_julia]{et_julia()}}: these run inside the forward
simulation, not inside the gradient, so there is no autodiff subset to
respect and nothing is gained by adding a transpiled R form. \code{model} in every
signature below is the fitted parameter NamedTuple (what \code{fit$draws} rows
become one at a time); \code{data} is the (possibly truncated) \code{DATA} object;
state codes are 1-based positions in the model's state vector.
}
