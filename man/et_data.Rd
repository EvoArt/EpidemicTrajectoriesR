% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{et_data}
\alias{et_data}
\title{Build the model's fixed structure and tracked state.}
\usage{
et_data(
  n_individuals,
  n_timepoints,
  transitions,
  starting_state,
  aggregates,
  group = NULL,
  observation_process = NULL,
  observation_weight = NULL,
  likelihood_weight = NULL,
  sampling_period = NULL,
  affected_individuals = NULL,
  coupled_transitions = NULL,
  coupling_transitions = NULL,
  rest_contribution = NULL,
  focal_self_contribution = TRUE,
  extras = list(),
  helpers = list()
)
}
\arguments{
\item{n_individuals, n_timepoints}{Problem size.}

\item{transitions}{An \code{\link[=et_transitions]{et_transitions()}} spec.}

\item{starting_state}{A function \verb{(model, data, X, i, t)} returning the
probability vector over states at the individual's first timepoint.}

\item{aggregates}{An \code{\link[=et_aggregate]{et_aggregate()}} declaration.}

\item{group}{Integer group index per individual. Used only by the default
\code{affected_individuals} and by whatever your own functions read off it.}

\item{observation_process}{A function \verb{(model, data, X, i, t)} returning a
per-state weight VECTOR.}

\item{observation_weight}{A function \verb{(model, data, X, i, t, s)} returning the
SCALAR weight for state \code{s}. \strong{This is the fast form} -- the likelihood needs
only one entry, so the scalar avoids allocating a weight vector per cell
under AD. Supply either or both; see ET's \code{epidemic_data} docs for when both
are needed.}

\item{likelihood_weight}{A function \verb{(model, data, X, i, t, s)} giving the
observation factor scored in the LIKELIHOOD, when that differs from the
filter's \code{observation_weight}. This is ET's seam for keeping some
observation parameters conjugate: a process that factorises
multiplicatively (\verb{w = capture x tests}) gives the PRODUCT as
\code{observation_weight}, so the filter sees everything, and only the
non-conjugate FACTOR here. Because the weights multiply, the log-likelihood
is a sum of the factors' contributions, so dropping one drops exactly its
term. \strong{Keeping a factor in both this and a conjugate block double-counts
it} - the conjugate parameter must not appear in \code{likelihood_weight}.
Defaults to \code{observation_weight}.}

\item{sampling_period}{A two-column matrix (\code{first}, \code{last}) per individual.
Defaults to the whole window for everyone.}

\item{affected_individuals}{Who each individual's state affects. \code{NULL} uses
groupmates under a fixed \code{group}; pass \code{\link[=et_affected_from_groups]{et_affected_from_groups()}} for
time-varying membership, or an \code{\link[=et_julia]{et_julia()}} expression for anything else.}

\item{coupled_transitions}{List of \code{c(from, to)} pairs naming which of a
neighbour's transitions this individual can influence -- purely an
optimisation, and often a large one.}

\item{coupling_transitions}{An optional separate \code{\link[=et_transitions]{et_transitions()}} used for
the coupling term only (ET's \code{coupling_trans_mat}). Power users only.}

\item{rest_contribution}{An \code{\link[=et_julia]{et_julia()}} replacing the whole coupling term.}

\item{focal_self_contribution}{See ET's \code{epidemic_data}; leave \code{TRUE} unless
your rate functions already do the focal's own accounting.}

\item{extras}{Named list of your own arrays, reachable as \code{data$name}.}

\item{helpers}{List of \code{\link[=et_helper]{et_helper()}} declarations callable from any body.}
}
\value{
An object of class \code{et_data}.
}
\description{
Build the model's fixed structure and tracked state.
}
