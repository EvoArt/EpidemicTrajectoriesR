% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectories.R
\name{et_convert_trajectories}
\alias{et_convert_trajectories}
\title{Convert an archived trajectory to R format.}
\usage{
et_convert_trajectories(template, keep_jld2 = FALSE, quiet = FALSE)
}
\arguments{
\item{template}{The path given to \code{et_sample(save_x =)}, with either
extension (e.g. \code{"X.rds"} or \code{"X.jld2"}). The per-flush files are found by
their \verb{_iters_x_to_y} names, so the template itself need not exist.}

\item{keep_jld2}{Keep the Julia originals instead of deleting them as they
are converted.}

\item{quiet}{Suppress progress messages.}
}
\value{
Invisibly, the paths of the \code{.rds} chunks in sweep order.
}
\description{
\code{\link[=et_sample]{et_sample()}} streams the latent trajectory through Julia, whose disk backend
writes \code{.jld2}. This rewrites that archive as \code{.rds}, which R reads natively,
and removes the Julia originals.
}
\details{
\code{et_sample(save_x = "X.rds")} calls this for you once sampling finishes. Call
it yourself when a run was interrupted: every flush already on disc is a
complete, independently readable file, so a crashed fit's trajectories are
still recoverable by pointing this at the template it was writing.

Chunks are converted one at a time, and a source file is deleted only once
its \code{.rds} counterpart exists -- so an interrupted conversion loses nothing
and can simply be re-run. Only one chunk is ever held in memory.
}
