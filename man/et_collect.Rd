% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{et_collect}
\alias{et_collect}
\title{Score residuals during the fit, without ever storing a trajectory.}
\usage{
et_collect(
  model,
  blocks = list(),
  residuals,
  n_sweeps = 1000,
  n_burn = 0,
  n_adapts = 0,
  seed = 1,
  x_init = NULL,
  adtype = "forwarddiff",
  n_keep = 500,
  thin = NULL,
  quiet = FALSE
)
}
\arguments{
\item{model}{An \code{\link[=et_model]{et_model()}}.}

\item{blocks}{Sampler blocks, as for \code{\link[=et_sample]{et_sample()}}.}

\item{residuals}{List of \code{\link[=et_residual_survival]{et_residual_survival()}} / \code{\link[=et_residual_waiting]{et_residual_waiting()}}.}

\item{n_sweeps, n_burn, n_adapts, seed, x_init, adtype}{As \code{\link[=et_sample]{et_sample()}}.}

\item{n_keep}{How many scored draws to store. 500 is far more than a
uniformity test needs; the collector stops storing once it has this many.}

\item{thin}{Score every \code{thin}-th sweep. Defaults to spreading \code{n_keep} draws
over the whole run.}

\item{quiet}{Suppress progress messages.}
}
\value{
A list with \code{fit} (an \code{\link[=et_fit]{et_fit()}}, without an archive) and \code{residuals}
(the same data frame \code{\link[=et_residuals]{et_residuals()}} returns).
}
\description{
The bounded-memory alternative to \code{et_sample(save_x =)} + \code{\link[=et_residuals]{et_residuals()}}.
Each draw is scored as it is produced and only the residual values are kept,
so nothing about the trajectory is retained.
}
\details{
Use this when the archive would be too large to keep: the trajectory is
\verb{n_timepoints x n_individuals} integers EVERY sweep, and \code{save_every} is a
flush interval rather than thinning, so a long run over a large population
archives everything. Storage here is \verb{n_individuals x n_keep} doubles per
residual instead -- on the badger model ~9.5 MB per residual against ~15 GB
of trajectories.

The trade is that the residuals must be decided BEFORE the fit. Archiving
lets you add new ones later without refitting; this does not.
}
