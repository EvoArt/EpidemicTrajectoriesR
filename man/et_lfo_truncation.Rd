% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfo.R
\name{et_lfo_truncation}
\alias{et_lfo_truncation}
\title{Declare how each \code{extras} entry behaves under truncation.}
\usage{
et_lfo_truncation(
  clamp = character(),
  filter = character(),
  copy = character(),
  keep = character(),
  strict = TRUE
)
}
\arguments{
\item{clamp}{Character vector of \code{extras} names to clamp at the cutoff
(\code{min(v, cutoff)}) -- for "last known present" vectors.}

\item{filter}{Character vector of \code{extras} names holding \verb{(id, time)} event
lists, to drop entries after the cutoff.}

\item{copy}{Character vector of \code{extras} names to copy unchanged, for
arrays a sampler mutates in place.}

\item{keep}{Character vector of \code{extras} names carried through untouched: an
explicit "this is a covariate, not a leak".}

\item{strict}{Error on an undeclared, time-shaped extra (the default and the
safe choice).}
}
\value{
An object of class \code{et_lfo_truncation}.
}
\description{
R mirror of ET's \code{truncation()}. Every entry of the model's \code{extras} (and
\code{group}, if time-varying information is smuggled into it) must be accounted
for; an undeclared entry that looks time-indexed is a Julia-side error rather
than a silent leak of post-cutoff information into a training fit.
}
