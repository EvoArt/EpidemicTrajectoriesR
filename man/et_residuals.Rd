% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residuals.R
\name{et_residuals}
\alias{et_residuals}
\title{Compute residuals from a fit's archived trajectories.}
\usage{
et_residuals(fit, residuals, sync_aggregates = TRUE, seed = 1)
}
\arguments{
\item{fit}{An \code{\link[=et_fit]{et_fit()}} from \code{\link[=et_sample]{et_sample()}} run with \verb{save_x =}.}

\item{residuals}{List of \code{\link[=et_residual_survival]{et_residual_survival()}} / \code{\link[=et_residual_waiting]{et_residual_waiting()}}.}

\item{sync_aggregates}{Rebuild the tracked arrays for each draw before scoring
it. Needed when any rate reads \code{data$aggregates}: a spec-derived hazard
then depends on them agreeing with this draw's trajectory, not with whatever
was current when the fit ended. Costs one pass per draw, which is why it is
not unconditional.}

\item{seed}{RNG seed for the randomised PIT. Its own stream, so it never
perturbs anything else.}
}
\value{
A data frame with one row per residual per scoreable individual per
draw: \code{residual}, \code{draw}, \code{individual}, \code{value}. \code{NA} where an individual
had no scoreable event.
}
\description{
Reads the trajectories \code{\link[=et_sample]{et_sample()}} streamed to disc, pairs each with the
parameter draw that produced it, and evaluates the residuals.
}
\details{
Requires the fit to have been run with \verb{save_x =}. Computing residuals
post-hoc means they can be recomputed, or new ones added, without refitting.
}
