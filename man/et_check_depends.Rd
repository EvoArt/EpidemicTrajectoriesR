% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{et_check_depends}
\alias{et_check_depends}
\title{Check the derived \verb{depends=} annotations against the model's behaviour.}
\usage{
et_check_depends(
  model,
  blocks = list(),
  factor = 0.7,
  x_init = NULL,
  tol = 1e-08
)
}
\arguments{
\item{model}{An \code{\link[=et_model]{et_model()}}.}

\item{blocks}{The sampler blocks, as for \code{\link[=et_loglik]{et_loglik()}}.}

\item{factor}{Multiplicative perturbation. Shrinks toward zero by default so
probability parameters stay inside their support.}

\item{x_init}{Optional starting trajectory to evaluate at.}

\item{tol}{Absolute tolerance in log-density units.}
}
\value{
A data frame with one row per (parameter, term): the declared
membership, the observed change, and a verdict. Invisibly, plus a message
summarising. Rows with \code{verdict == "UNDER-DECLARED"} are bugs.
}
\description{
For each likelihood term, perturbs every parameter \strong{not} in that term's
declared dependency set and asserts the term's value does not change. A term
that does change has an under-declared \verb{depends=}.
}
\details{
This is a probe, not a proof: a dependency that happens to be inactive at the
tested parameter values would pass. Run it at more than one \code{factor}, and at a
realistic \code{x_init}, if the model has regime-dependent structure.
}
