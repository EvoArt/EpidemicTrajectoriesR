% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{et_reproduction_numbers}
\alias{et_reproduction_numbers}
\title{Case reproduction numbers, R_i.}
\usage{
et_reproduction_numbers(
  fit,
  components,
  secondary,
  infection = c("S", "E"),
  infectious_state = "I",
  weight = NULL,
  draws = NULL
)
}
\arguments{
\item{fit}{An \code{\link[=et_fit]{et_fit()}} from \code{\link[=et_sample]{et_sample()}} run with \verb{save_x =}.}

\item{components}{Named list of rate functions -- the force-of-infection
decomposition. Each is written in the same R subset as any other rate
function, taking \verb{(model, data, i, t)}.}

\item{secondary}{Name of the component that is transmission (the part
attributable to other individuals).}

\item{infection}{The transition marking a new infection, e.g. \code{c("S", "E")}.}

\item{infectious_state}{State from which an individual can infect others.}

\item{weight}{Optional relative infectiousness, a function
\verb{(model, data, X, i, t)}. Defaults to uniform, i.e. equal attribution.}

\item{draws}{Which archived draws to use. Defaults to all of them.}
}
\value{
A data frame with one row per individual per draw: \code{draw},
\code{individual}, \code{value} (\code{NA} where the individual was never infectious).
}
\description{
The expected number of secondary infections caused by each individual, from a
fit's archived trajectories.
}
\details{
\strong{This is not a residual, and it is not computed one individual at a time.}
Attributing an infection needs to know every infective present in the victim's
group at that moment, so R_i takes a pass over the whole population before any
one individual's value is known. That is why it has its own entry point rather
than joining \code{\link[=et_residuals]{et_residuals()}}.
\subsection{The declared decomposition}{

The package cannot split a force of infection by itself: \code{S -> E} is one rate
function returning one number, and nothing in it says which part is background
and which is transmission. So you declare the parts, exactly as you declare
rates and aggregates -- as ordinary R rate functions of \verb{(model, data, i, t)}
-- and say which one is transmission via \code{secondary}. The package sums them,
normalises, and never needs to know what any of them means.

For each infection event, the transmission component of the victim's force of
infection is shared among the infectives in its group in proportion to
\code{weight}. Background infections are attributed to nobody, which is the point
of the split. An individual that was never infectious gets \code{NA} -- it had no
opportunity to infect anyone, which differs from having had the opportunity
and infected nobody (a genuine \code{0}).
}
}
