% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{et_setup}
\alias{et_setup}
\title{Start the Julia session and load the modelling packages.}
\usage{
et_setup(
  julia_home = NULL,
  project = NULL,
  dev = NULL,
  ad_backends = NULL,
  verbose = FALSE,
  force = FALSE
)
}
\arguments{
\item{julia_home}{Directory containing the \code{julia} binary. \code{NULL} lets
JuliaCall find it (respecting \code{JULIA_HOME}).}

\item{project}{Path to the Julia project to activate. Defaults to the pinned
environment shipped with this package.}

\item{dev}{Named character vector of local checkouts to \code{Pkg.develop}, e.g.
\code{c(EpidemicTrajectories = "~/.julia/dev/EpidemicTrajectories")}. Use this
when benchmarking or debugging local source changes -- without it the pinned
GitHub versions are used, and a local edit is invisible.}

\item{ad_backends}{Extra automatic-differentiation backends to resolve and
load, e.g. \code{"mooncake"}. Forward mode works out of the box; the
reverse-mode backends are large, so they are opt-in. See \code{\link[=et_adtype]{et_adtype()}}.}

\item{verbose}{Print Julia's setup chatter.}

\item{force}{Re-run even if the session is already up.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
Start the Julia session and load the modelling packages.
}
