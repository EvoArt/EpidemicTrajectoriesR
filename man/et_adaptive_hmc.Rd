% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blocks.R
\name{et_adaptive_hmc}
\alias{et_adaptive_hmc}
\title{A fixed-trajectory HMC block that adapts its metric and step size.}
\usage{
et_adaptive_hmc(
  vars,
  target_accept = 0.8,
  n_steps = 15,
  metric = c("diagonal", "dense", "unit")
)
}
\arguments{
\item{vars}{Character vector of parameter names sampled together.}

\item{target_accept}{Target acceptance rate for step-size dual averaging.}

\item{n_steps}{Leapfrog steps per iteration, in warm-up and sampling alike.}

\item{metric}{\code{"diagonal"} (the default), \code{"dense"} or \code{"unit"}. A dense
metric learns correlations between parameters, at the cost of estimating
many more entries during warm-up, worth it when the posterior is strongly
correlated and you can afford a long warm-up.}
}
\value{
An \code{et_block}.
}
\description{
The usual choice alongside an iFFBS trajectory kernel, and the one to reach
for first.
}
\details{
Like \code{\link[=et_hmc]{et_hmc()}} it costs a constant \code{n_steps} leapfrog steps per iteration,
so the run has a flat predictable cost and no tree doubling. Unlike
\code{\link[=et_hmc]{et_hmc()}} it does not ask you for a step size: it adapts the mass matrix and
step size during warm-up exactly as NUTS does, then holds both fixed for
sampling.

That difference matters more than it sounds. A hand-tuned metric has to be
right in both order and scale, and a scale that is far too small produces a
chain that never moves while every acceptance diagnostic still looks healthy.
Adapting removes that failure mode.
}
\seealso{
\code{\link[=et_hmc]{et_hmc()}} for a fixed metric you supply yourself, \code{\link[=et_nuts]{et_nuts()}} for
tree-doubling with an adaptive trajectory length.
}
