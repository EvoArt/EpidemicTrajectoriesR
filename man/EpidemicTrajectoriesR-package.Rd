% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EpidemicTrajectoriesR-package.R
\docType{package}
\name{EpidemicTrajectoriesR-package}
\alias{EpidemicTrajectoriesR}
\alias{EpidemicTrajectoriesR-package}
\title{EpidemicTrajectoriesR: epidemic models from R, fitted in Julia}
\description{
Specify a discrete-time individual-level epidemic model with R objects, write
its rate and observation functions in a restricted subset of R, and fit it
with the Julia stack (\code{EpidemicTrajectories.jl}, \code{PracticalBayes.jl},
\code{PracticalEpiBayes.jl}) without writing Julia.
}
\section{The shape of a model}{

\enumerate{
\item \code{\link[=et_aggregate]{et_aggregate()}} -- the arrays tracked during the latent update, with
their reversible updates. The package attaches no meaning to them.
\item \code{\link[=et_transitions]{et_transitions()}} -- the states, the transitions between them, and the
rate function for each. Optionally an \code{\link[=et_survival]{et_survival()}} making every step
conditional on survival.
\item \code{\link[=et_data]{et_data()}} -- problem size, group structure, the observation process,
and your own arrays.
\item \code{\link[=et_model]{et_model()}} -- parameters, priors, and deterministic
reparameterisations.
\item \code{\link[=et_sample]{et_sample()}} -- Gibbs blocks and the run.
}
}

\section{Two things worth knowing up front}{

The likelihood has \strong{two halves} -- the transitions and the observations --
and both are needed. This package always emits both when an observation
process is given, and warns when one is not, because omitting the observation
term is a silent modelling bug: every observation parameter would then be
sampled from its prior with no data reaching it.

The \verb{depends=} annotations that let each Gibbs block skip likelihood terms it
cannot move are \strong{derived} from your function bodies, not declared by you.
\code{\link[=et_check_depends]{et_check_depends()}} verifies them.
}

\section{Escape hatches}{

\code{\link[=et_julia]{et_julia()}} accepts literal Julia anywhere a transpiled R function is
accepted; \code{\link[=et_kernel_julia]{et_kernel_julia()}} drops a raw Gibbs kernel into the sampler;
\code{\link[=et_eval]{et_eval()}} reaches anything in the Julia session; and \code{\link[=et_julia_source]{et_julia_source()}}
prints the generated module, which runs standalone.
}

\author{
\strong{Maintainer}: Arthur Newbury \email{a.r.newbury@exeter.ac.uk}

Authors:
\itemize{
  \item Arthur Newbury \email{a.r.newbury@exeter.ac.uk}
}

}
\keyword{internal}
