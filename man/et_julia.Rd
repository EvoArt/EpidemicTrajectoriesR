% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transpile_fn.R
\name{et_julia}
\alias{et_julia}
\title{Wrap literal Julia source.}
\usage{
et_julia(src, reads = NULL)
}
\arguments{
\item{src}{Julia source for the function BODY (not the signature, the
protocol's signature is generated around it).}

\item{reads}{Optional character vector of parameter names the body reads. When
given, \verb{depends=} derivation continues to work; it is then the caller's
responsibility to be exhaustive.}
}
\value{
An object of class \code{et_julia}.
}
\description{
Accepted anywhere a transpiled R function is. Use it for a construct the R
subset does not cover, or to hand-optimise one function without abandoning the
R model.
}
\details{
The body cannot be scanned for \code{model$x} reads, so any likelihood term
containing an \code{et_julia()} function is emitted \strong{without} a \verb{depends=}
annotation. That is the safe direction (PracticalBayes then evaluates the term
unconditionally, exactly as it would with no annotation at all), but it does
forgo the speed-up described in DESIGN.md section 5. Pass \code{reads} to declare
the parameters the body touches and keep the annotation.
}
