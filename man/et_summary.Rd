% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{et_summary}
\alias{et_summary}
\title{Declare a tracked array with a hand-written reversible update.}
\usage{
et_summary(type = c("Int", "Float64", "Bool"), dim, update)
}
\arguments{
\item{type}{Julia element type: \code{"Int"}, \code{"Float64"} or \code{"Bool"}.}

\item{dim}{Integer vector of dimensions.}

\item{update}{A function \verb{(model, data, X, state, i, t, reverse)}.}
}
\value{
An object of class \code{et_summary}.
}
\description{
The escape hatch from \code{\link[=et_aggregate]{et_aggregate()}}, for an update whose reverse cannot be
derived from the \code{A[idx] <- A[idx] + contribution} shape.
}
\details{
\code{update} is a function \verb{(model, data, X, state, i, t, reverse)} written in the
transpilable R subset. It must apply the contribution when \code{reverse} is
\code{FALSE} and remove exactly the same contribution when it is \code{TRUE}. Refer to
the array by name; the package allocates it.
}
\examples{
# How many S individuals in this group went on to E at t -> t+1. The
# contribution depends on X[t+1, i], so @aggregate cannot express it.
nSE <- et_summary("Int", c(34, 161),
  update = function(model, data, X, state, i, t, reverse) {
    g <- data$social_group[i, t]
    if (g > 0 && t < data$n_timepoints) {
      c <- (state == "S") && (X[t + 1, i] == "E")
      if (reverse) nSE[g, t] <- nSE[g, t] - c else nSE[g, t] <- nSE[g, t] + c
    }
  })
}
