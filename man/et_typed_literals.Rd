% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typed-literals.R
\name{et_typed_literals}
\alias{et_typed_literals}
\alias{et_one}
\alias{et_zero}
\alias{et_num}
\title{Typed literals for a transpiled body.}
\usage{
et_one()

et_zero()

et_num(x)
}
\arguments{
\item{x}{A value to convert.}
}
\value{
\code{1}, \code{0}, or \code{x}.
}
\description{
An accumulator started at a bare \code{1} is an integer, and becomes a
\verb{Union\{Int, Dual\}} in Julia the moment a parameter multiplies into it --
runtime dispatch inside the autodiff loop, which is the single most expensive
thing to get wrong in this stack. \code{et_one()} and \code{et_zero()} start it at the
automatic-differentiation scalar type instead, and \code{et_num()} converts a value
to it.
}
\details{
Use them for a value that will be combined with parameters. Do NOT use them
for an index or a loop counter -- those must stay integers.

In R these are the identity, so a function using them still runs unchanged
outside the transpiler.
}
\examples{
# w accumulates a product of test-parameter terms, so it starts typed:
f <- function(model, data, X, i, t, s) {
  w <- et_one()
  w * model$theta
}
}
